<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC "-//Hibernate/Hibernate Configuration DTD 3.0//EN" "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Database connection properties - Driver, URL, user, password -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/molarak?serverTimezone=UTC</property>
        <property name="hibernate.connection.username">dereck</property>
        <property name="hibernate.connection.password">Dodobird@4698</property>

        <!-- Connection Pool Size -->
        <property name="hibernate.connection.pool_size">5</property>

        <!-- org.hibernate.HibernateException: No CurrentSessionContext configured! -->
        <property name="hibernate.current_session_context_class">thread</property>

        <!-- Outputs the SQL queries, should be disabled in Production -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!--  Behavior to the database schema at the initialization of the session factory-->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapping with annotation - specify the annotated classes here -->

        
    </session-factory>
</hibernate-configuration>